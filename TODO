
 wrap Objective-C in Haskell (profile it)

 make map from key names to key codes

 make a Haskell server (one route for a list of word-rule pair)

 NarcissisticGrammar: exclusive and allResults

 set it all up on the work laptop

 Yesod: setSession for the dynamic list vocabulary on the client (e.g. all tokens in current Emacs buffer)

 Haskell: spawn another thread to keep polling the current application

